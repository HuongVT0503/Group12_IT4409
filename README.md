# Group12_IT4409


report thì cho user report user khác, report post của ngkh
sau đó admin nhận thông báo về report đó, admin cũng có table report trong db để lưu reporter-id,target-id,reason,status(pending,resolved,rejected)


admin cũng có danh sách tất cả user và posts, tổng số lượng users và posts
v th


netstat -ano | findstr :4000

taskkill /PID <PID> /F